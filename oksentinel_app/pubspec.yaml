name: oksentinel_app
description: OkSentinel Mobile App - Secure file sharing on Android
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI
  cupertino_icons: ^1.0.6
  
  # State Management
  provider: ^6.1.5
  
 # Networking
  http: ^1.2.2
  
  # File Operations
  file_picker: ^6.2.1
  path_provider: ^2.1.4
  mime: ^1.0.6
  
  # Media  video_player: ^2.9.2
  photo_view: ^0.14.0
  
  # Storage
  shared_preferences: ^2.2.3
  
  # Utils
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
